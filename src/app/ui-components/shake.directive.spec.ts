import { ShakeDirective } from './shake.directive';

describe('ShakeDirective', () => {
  it('should create an instance', () => {
    const directive = new ShakeDirective();
    expect(directive).toBeTruthy();
  });
});
